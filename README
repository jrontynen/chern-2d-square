Calculates the Chern number of a 2D lattice. The batch file chern.sh submits the paralleized code, run_chern.m contains the parameters and calls chern.m to calculate the Chern number. After this, the script collect_chern.m is manually called to collect the data.
